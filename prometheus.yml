global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/api/actuator/prometheus'
    static_configs:
      - targets: ['api-biblioteca:8080']

# define a aplicação target, e o endpoint que vai pegar as métricas.